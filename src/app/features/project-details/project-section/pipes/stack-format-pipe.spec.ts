import {StackFormatPipe} from './stack-format-pipe';

describe('StackFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new StackFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
